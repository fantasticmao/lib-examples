/**
 * 练习 5-6
 *
 * 采用指针而非数组索引方式改写章节和练习中的某些程序，例如 getline
 * （第 1、4 章），atoi、itoa 以及它们的变体形式（第 2、3、4 章），reverse（第 3
 * 章）， strindex、getop（第 4 章） 等等。
 */

char *getlint();

int atoi();

char *itoa();

void reverse();

int strindex(char *s);

int main(int argc, char *argv[]) { return 0; }